
 const add = (first, second)=>{
    return first+second;
}
 const multiply = (first , second)=>{
    return first*second;
}
const dividetheFirstnumberByTheSecond=(first,second)=>{
    return first/second;
}
export {add,multiply};
// way of export
// 1.export default add;
// 2.export {add,multiply}